# Lovable Cloud auto-generates these on remix — no need to fill in manually.
VITE_SUPABASE_PROJECT_ID=""
VITE_SUPABASE_PUBLISHABLE_KEY=""
VITE_SUPABASE_URL=""

# Optional: set these to brand the static index.html / sitemap / emails at build time.
# Edge functions also read PUBLIC_SITE_NAME / PUBLIC_SITE_URL / SENDER_DOMAIN.
VITE_BASE_URL=""
VITE_PAYMENTS_CLIENT_TOKEN=""
